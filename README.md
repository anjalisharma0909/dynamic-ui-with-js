👤 Dynamic User Cards Project

A clean and responsive Dynamic User Cards Web App built using HTML, CSS, and JavaScript, where user profile cards are generated dynamically from a JavaScript data array.

This project demonstrates DOM manipulation, array iteration, and modern UI layout using Flexbox.

🚀 Features

🔹 Dynamic card generation using JavaScript

🔹 Data-driven UI (no hardcoded cards)

🔹 Responsive card layout using Flexbox

🔹 Clean & minimal UI design

🔹 Easy to scale (just add more users in array)

🛠️ Tech Stack

HTML5 – Structure of the web page

CSS3 – Styling & responsive layout

JavaScript (ES6) – Dynamic content rendering & DOM manipulation

⚙️ How It Works

-User data is stored in a JavaScript array as objects

-forEach() loop dynamically creates HTML card templates

-Cards are injected into the <main> container using innerHTML

-Layout is managed using Flexbox for responsiveness
